name: CI

on:
  push:
    branches:
      - "7.0"

env:
  # Mirror GitLab CI variable
  SONARCLOUD_ENABLED: ${{ vars.SONARCLOUD_ENABLED || 'false' }}
  PORTAINER_URL: ${{ secrets.PORTAINER_URL }}
  PORTAINER_API_TOKEN: ${{ secrets.PORTAINER_API_TOKEN }}
  IO_PASSWORD: ${{ secrets.IO_PASSWORD }}

jobs:
  deploy-test:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Deploy test stack
        run: |
          chmod +x ./others/cicd/simci
          export PORTAINER_API_TOKEN="${PORTAINER_API_TOKEN}"
          export IO_PASSWORD="${IO_PASSWORD}"
          ./others/cicd/simci portainer-stack-delete gitlab-simfeatures "$PORTAINER_URL"
          ./others/cicd/simci portainer-stack-deploy -f ./others/cicd/portainer-stack.yml gitlab-simfeatures "$PORTAINER_URL"

  unit-tests:
    runs-on: ubuntu-latest
    needs: deploy-test
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Run unit tests
        run: |
          chmod +x ./others/cicd/simci
          export PORTAINER_API_TOKEN="${PORTAINER_API_TOKEN}"
          export IO_PASSWORD="${IO_PASSWORD}"
          ./others/cicd/simci simplicite-run-unit-tests SimFeatures gitlab-simfeatures "$PORTAINER_URL"
          ./others/cicd/simci portainer-stack-get-coverage -v gitlab-simfeatures "$PORTAINER_URL"

      - name: Upload coverage report
        uses: actions/upload-artifact@v4
        with:
          name: jacoco-report
          path: jacoco.xml
          if-no-files-found: error

  sonarcloud-check:
    runs-on: ubuntu-latest
    needs: unit-tests
    if: env.SONARCLOUD_ENABLED == 'true'
    env:
      MAVEN_OPTS: -Dmaven.repo.local=.m2/repository
      SONAR_TOKEN: ${{ secrets.SONAR_TOKEN }}
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Set up Java 21
        uses: actions/setup-java@v4
        with:
          distribution: temurin
          java-version: '21'

      - name: Set up Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '20'

      - name: Cache Maven and SonarCloud files
        uses: actions/cache@v4
        with:
          path: |
            .sonar/cache
            .m2/repository
          key: ${{ runner.os }}-maven-${{ hashFiles('pom.xml') }}
          restore-keys: |
            ${{ runner.os }}-maven-

      - name: Download coverage report
        uses: actions/download-artifact@v4
        with:
          name: jacoco-report

      - name: Run SonarCloud analysis
        run: |
          mvn -B verify org.sonarsource.scanner.maven:sonar-maven-plugin:sonar \
            -Dsonar.projectKey=module-simfeatures \
            -Dsonar.coverage.jacoco.xmlReportPaths=jacoco.xml

